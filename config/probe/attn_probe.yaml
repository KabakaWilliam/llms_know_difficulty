# Attention Probe Configuration
# Inherits from torch_probe_default.yaml

defaults:
  - torch_probe_default

#@package _global_

# All parameters inherited from torch_probe_default
# No additional parameters needed for this probe type

probe_class:
  _target_: llms_know_difficulty.probe.torch_probe.AttnLite